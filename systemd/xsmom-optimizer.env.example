# /etc/default/xsmom-optimizer
# Edit and move to /etc/default/xsmom-optimizer

# Path to the repo venv (optional)
VENV=/opt/xsmom-bot/venv

# Live config to update (the optimizer will backup before writing)
CONFIG_PATH=/opt/xsmom-bot/config/config.yaml

# Grid defining the search space
GRID_PATH=/opt/xsmom-bot/config/optimizer.grid.yaml

# Objective & risk caps
OBJECTIVE=calmar_minus_lambda_turnover
LAMBDA_TURNOVER=0.001
MAX_DD=0.45
MAX_VOL=0.60

# Restart the live service after write (optional)
RESTART_SERVICE=xsmom-bot
