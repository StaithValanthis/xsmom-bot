# === Copy to .env and edit ===

# Bybit API
BYBIT_API_KEY=your_key_here
BYBIT_API_SECRET=your_secret_here

# Optional: run testnet mode via script args instead (not env)
# BYBIT_TESTNET=true

# Python runtime
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1

# Bot runtime controls (override with CLI flags if you prefer)
# DRY_RUN=true  # set to "false" to place real orders (or use --live without --dry)
